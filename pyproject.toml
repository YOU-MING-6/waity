[project]
name = "shutdowntool"
version = "0.1.0"
description = "Scheduled Shutdown Reminder Tool"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pyside6-fluent-widgets>=1.10.4",
]
